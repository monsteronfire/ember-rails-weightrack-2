<ul>
  {{#each user in model}}
    <li>
      {{#link-to 'user' user}}
        {{user.lastName}},
        {{user.firstName}}
      {{/link-to}}
    </li>
  {{else}}
    <li>No users found.</li>
  {{/each}}
</ul>
